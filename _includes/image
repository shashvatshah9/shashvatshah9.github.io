<figure>
  <img class="image" width="{{ include.w }}" src="{{ include.url | prepend: site.baseurl | prepend: site.url}}"
    {% if include.caption %}
    alt="{{ include.caption }}"
    {% endif %}
  >
  {% if include.caption %}
  <figcaption class="image-caption">{{ include.caption }}</figcaption>
  {% endif %}
</figure>

